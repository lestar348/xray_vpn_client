package com.synaptic.xcorevpn.services.network

object ApiList {
    const val domenAPI = "https://money.payfaraway.ru/"

    const val apiVersion = "v1"

    const val regUser = "api/$apiVersion/reg_user"

    const val confirmEmail = "api/$apiVersion/confirm_email"

    const val getServers = "api/$apiVersion/get_servers"

    const val checkStatus = "api/$apiVersion/check_status"

    const val del_user = "api/$apiVersion/del_user"
}